# Legacy design references

Preserved from the old top-level workspace (deleted in Phase 6 of
`../05-migration-plan.md`) for historical context — the design intent behind
the old engine's tuning/board work, referenced while writing the six design
docs in `../`. Not maintained, not necessarily accurate against the new
`simharness_v2` engine.

- `BOARD_IMPLEMENTATION_PLAN.md` — the original board-package design intent
  (`dnd_board`, kept and extended, not retired — see `../00-architecture-overview.md`).
- `otyugh_CR7_TUNING_GUIDE.md` — tuning notes for a higher-CR otyugh variant,
  never migrated to a `sims/` sim in this rewrite.
- `dnd5e_combat_README.md` — the old engine's own architecture README,
  superseded by `../00-architecture-overview.md` through `../06-implementation-guide.md`.
