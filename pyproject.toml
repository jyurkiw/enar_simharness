[project]
name = "simulations-workspace"
version = "0.1.0"
requires-python = ">=3.13"

[tool.uv.workspace]
members = [
    "simharness",
    "../dnd_board",
    "dnd5e_data",
    "dnd5e",
]    # dnd5e added in Phase 3
